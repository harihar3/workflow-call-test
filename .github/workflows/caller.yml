name: TEST Caller Pipeline

on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]
  push:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/reusable.yml
    with:
      trigger-event: ${{ github.event_name }}
